# Core framework
fastapi>=0.115.0
uvicorn>=0.30.6

# HTTP client (talk to Retell API)
httpx>=0.27.0

# Async SQLite (no separate server needed)
aiosqlite>=0.20.0

# Environment variables
python-dotenv>=1.0.0

# Data validation — use latest which has Python 3.14 wheels
pydantic>=2.10.0
